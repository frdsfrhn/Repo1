namespace RomMemoryScanner.Core.Models;

/// <summary>Consoles supported per requirements §3 (v1). GBA/N64 are stretch goals and intentionally omitted.</summary>
public enum ConsoleType
{
    Unknown,
    Snes,
    Genesis,
    Ps1,
}
