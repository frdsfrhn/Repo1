<UserControl x:Class="RomMemoryScanner.App.Views.LiveScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RomMemoryScanner.App.Views">
    <UserControl.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Live value scan" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
        <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,12"
                   Text="Don't know an address yet? Tell the app a value you can see on screen (e.g. your HP), scan, then tell it how the value changed after taking damage or healing. Repeat until one address is left, then tag it." />

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,8,0,0" TextWrapping="Wrap" />

        <StackPanel DockPanel.Dock="Top">

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Data type:" VerticalAlignment="Center" Margin="0,0,4,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableDataTypes}" SelectedItem="{Binding DataType}" IsEnabled="{Binding HasActiveScan, Converter={StaticResource InverseBooleanConverter}}" />
                <TextBlock Grid.Column="2" Text="Byte order:" VerticalAlignment="Center" Margin="12,0,4,0" />
                <ComboBox Grid.Column="3" ItemsSource="{Binding AvailableByteOrders}" SelectedItem="{Binding ByteOrder}" IsEnabled="{Binding HasActiveScan, Converter={StaticResource InverseBooleanConverter}}" />
            </Grid>

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Known value (decimal):" VerticalAlignment="Center" Margin="0,0,4,0" />
                <TextBox Grid.Column="1" Text="{Binding KnownValueText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding HasActiveScan, Converter={StaticResource InverseBooleanConverter}}" />
                <Button Grid.Column="2" Content="Start scan" Command="{Binding StartScanCommand}" Margin="8,0,0,0" Padding="16,4" />
            </Grid>

            <Grid Margin="0,0,0,8" Visibility="{Binding HasActiveScan, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Then it..." VerticalAlignment="Center" Margin="0,0,4,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableComparisons}" SelectedItem="{Binding Comparison}" />
                <TextBlock Grid.Column="2" Text="New value:" VerticalAlignment="Center" Margin="12,0,4,0" />
                <TextBox Grid.Column="3" Text="{Binding NewValueText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="4" Content="Next scan" Command="{Binding NextScanCommand}" Margin="8,0,0,0" Padding="16,4" />
            </Grid>

            <Button Content="Reset" Command="{Binding ResetCommand}" HorizontalAlignment="Left" Padding="12,4" Margin="0,0,0,8" />

            <TextBlock Text="{Binding CandidateCount, StringFormat='{}{0} candidate(s)'}" FontWeight="Bold" Margin="0,0,0,4" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Candidates}" SelectedItem="{Binding SelectedCandidate}"
                      AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Address" Binding="{Binding ConsoleAddress, StringFormat=X6}" Width="120" />
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Tag selected as:" Margin="0,0,0,4" />
                <TextBox Text="{Binding TagLabel, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />
                <Button Content="Tag → send to Dashboard" Command="{Binding TagSelectedCommand}" Padding="12,4" HorizontalAlignment="Left" />
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
